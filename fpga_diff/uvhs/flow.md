# UVHS Hejian FPGA Debug Flow

The active UVHS + Hejian FPGA + `fpga-host` debug runbook has moved to the
MinJie documentation:

```text
docs/FpgaDiff/uvhs-host-debug.md
```

Use that document for the current host split, raw BAR probe order, official
XDMA driver gate, MinJie driver adaptation gate, and `fpga-host` bring-up
sequence. Keep this file as a pointer so the env-scripts checkout does not carry
a second copy of the live flow.
